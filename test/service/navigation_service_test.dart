import 'package:flutter_test/flutter_test.dart';

void main() {
  test('when `navigateTo` called pushes the new route', () async {
    fail("Not implemented");
  });

  test('when `navigateToReplacing` called pushes the new route and replaces the current screen', () async {
    fail("Not implemented");
  });

  test('when `goBack` called pops the topmost route', () async {
    fail("Not implemented");
  });
}
